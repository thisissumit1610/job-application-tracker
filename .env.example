# libSQL connection string.
#   local file  -> file:./data/tracker.db   (the default if unset)
#   Turso       -> libsql://<db>-<org>.turso.io
DATABASE_URL=file:./data/tracker.db

# Only needed for a hosted Turso database.
# DATABASE_AUTH_TOKEN=
